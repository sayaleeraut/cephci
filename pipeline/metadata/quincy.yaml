#=====================================================================================
# Metadata file for quincy upstream testingn.
# Single file to specify test suites to be executed for all regression (sanity) and schedule tests
# as per defined in each stages.
# Each stage will execute in sequential pattern.
# Contains all Default parameter used for Execution when no override is specified.
# parameter required to create a similar custom file is suite name, suite yaml file, global configuration file,
# platform, rhbuild, inventory and metadata information like frequency of execution, tier, cloud type, functional group and stage.
#=====================================================================================
- name: "Beginning Red Hat CephFS Core Feature testing with upstream build"
  suite: "suites/quincy/cephfs/<suite_file>.yaml"
  global-conf: "conf/quincy/cephfs/<conf_file>.yaml"
  platform: "rhel-8"
  rhbuild: "quincy"
  inventory:
    openstack: "conf/inventory/rhel-8-latest.yaml"
  metadata:
    - sanity
    - openstack
    - cephfs
    - stage-1

- name: "Beginning Red Hat Ceph RBD core feature testing with upstream build"
  suite: "suites/quincy/rbd/<suite_file>.yaml"
  global-conf: "conf/quincy/rbd/<conf_file>.yaml"
  platform: "rhel-8"
  rhbuild: "quincy"
  inventory:
    openstack: "conf/inventory/rhel-8-latest.yaml"
  metadata:
    - sanity
    - openstack
    - rbd
    - stage-1

- name: "Beginning Red Hat Ceph RGW Core feature testing with upstream build"
  suite: "suites/quincy/rgw/<suite_file>.yaml"
  global-conf: "conf/quincy/rgw/<conf_file>.yaml"
  platform: "rhel-8"
  rhbuild: "quincy"
  inventory:
    openstack: "conf/inventory/rhel-8-latest.yaml"
  metadata:
    - sanity
    - openstack
    - rgw
    - stage-1

- name: "Beginning Red Hat Ceph Adm testing with upstream build"
  suite: "suites/quincy/cephadm/<suite_file>.yaml"
  global-conf: "conf/quincy/cephadm/<conf_file>.yaml"
  platform: "rhel-8"
  rhbuild: "quincy"
  inventory:
    openstack: "conf/inventory/rhel-8-latest.yaml"
  metadata:
    - sanity
    - openstack
    - dmfg
    - stage-1
